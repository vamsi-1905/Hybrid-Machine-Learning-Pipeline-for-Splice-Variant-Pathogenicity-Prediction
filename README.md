An end to end project to perform splice site prediction and severity prediction via a multi model implementation. XGBOOST and DNABERT both were trained and fine tuned on our data.
An ensemble was built combining both for better holistic predictions and improved model accuracy
Front End was built via React and JSX while using FASTAPI for making inferences from the pre trained models via the saved model weights
